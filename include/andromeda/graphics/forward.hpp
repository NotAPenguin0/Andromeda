#pragma once

// Header with forward declarations for common types like gfx::Mesh and gfx::Texture.
// Useful if you only need handles to a type.

namespace andromeda::gfx {
	
class Context;

struct Mesh;
struct Texture;
struct Material;

} // namespace andromeda::gfx